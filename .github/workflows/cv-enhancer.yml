# =============================================================================
# .github/workflows/cv-enhancer.yml
# -----------------------------------------------------------------------------
# RESERVED — future AI automation pipeline for the CV Enhancer tool.
#
# This is an intentional placeholder so the automation slot exists in the repo.
# The "AI Tools" button currently points to under-construction.html; once the
# tool is ready, this workflow will process an uploaded CV with Gemini.
#
# Planned trigger + secrets (not active yet):
#   on: repository_dispatch  ->  types: [cv-enhancer-request]
#   secrets.LLM_API_KEY      ->  mapped to GEMINI_API_KEY in the job
# =============================================================================

name: CV Enhancer (reserved)

# Manual-only for now so nothing runs by accident.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Placeholder — pipeline not yet implemented
    runs-on: ubuntu-latest
    steps:
      - name: Announce reserved status
        run: |
          echo "🚧 CV Enhancer pipeline is reserved and not yet implemented."
          echo "The AI Tools button currently links to under-construction.html."
